Edit Image 1 while preserving its good left-to-right whiteboard process, icons, status colors, and output list.

Required corrections:
- Remove the large centered boxed title. Put small handwritten text "结论与证据分开核对" at the top-left with no surrounding box.
- The four status boxes must behave as one result group. Draw the output fan-out from the result group as a whole, not from only "部分核验 ~".
- Replace the four dashed annotation boxes at the bottom with unboxed small line-icon callouts and leader lines. Exact labels: "原结论正文保留", "固定原因码", "传输状态 + 尝试次数", "只重试临时错误".
- Keep the main exact flow: "用户请求" → "计划与工具" → "证据台账" → "候选结论 + 声明" → "禁用工具的语义复核".
- Keep outputs: "Web", "CLI", "HTML 报告", "快照", "JSON · NDJSON".

Constraints: opaque pure white background; Simplified Chinese; orthogonal arrows; no standalone title block; no colored fills, shadows, gradients, or decorative elements.
