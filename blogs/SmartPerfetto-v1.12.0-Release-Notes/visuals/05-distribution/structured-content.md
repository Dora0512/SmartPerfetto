# Structured content

- Center: SmartPerfetto v1.12.0.
- Spoke 1: GitHub Release → Windows x64 / macOS arm64 / Linux x64.
- Spoke 2: npm CLI → `@gracker/smartperfetto@1.12.0` → `smp doctor`.
- Spoke 3: Docker → `w553000664/smartperfetto:1.12.0` → amd64 / arm64.
- Spoke 4: source → `v1.12.0` tag → `./start.sh`.
- Footer boundary: update check only notifies; it does not install, overwrite, restart, or migrate.
